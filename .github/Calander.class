// Source code is decompiled from a .class file using FernFlower decompiler.
public class Calendar {
   static int dayOfMonth = 1;
   static int month = 1;
   static int year = 1902;
   static int dayOfWeek = 4;
   static int nDaysInMonth = 31;

   public Calendar1() {
   }

   public static void main(String[] var0) {
      int var1 = 0;
      int var2 = 0;

      while(year < 2000) {
         if (dayOfWeek == 1) {
            System.out.println(dayOfMonth + "/" + month + "/" + year + " Sunday");
            if (dayOfMonth == 1) {
               ++var2;
            }
         } else {
            System.out.println(dayOfMonth + "/" + month + "/" + year);
         }

         advance();
         ++var1;
         if (var1 == 365) {
            break;
         }
      }

      System.out.println("During the 20th century, " + var2 + " Sundays fell on the first day of the month");
   }

   private static void advance() {
      if (dayOfWeek == 7) {
         dayOfWeek = 1;
      } else {
         ++dayOfWeek;
      }

      if (dayOfMonth == nDaysInMonth) {
         ++month;
         nDaysInMonth = nDaysInMonth(month, year);
         dayOfMonth = 1;
         if (month == 13) {
            month = 1;
            ++year;
         }
      } else {
         ++dayOfMonth;
      }

   }

   private static boolean isLeapYear(int var0) {
      boolean var1 = var0 % 400 == 0;
      var1 = var1 || var0 % 4 == 0 && var0 % 100 != 0;
      return var1;
   }

   private static int nDaysInMonth(int var0, int var1) {
      boolean var2 = false;
      byte var3;
      switch (var0) {
         case 1:
         case 3:
         case 5:
         case 7:
         case 8:
         case 10:
         case 12:
            var3 = 31;
            break;
         case 2:
            if (isLeapYear(var1) && var0 == 2) {
               var3 = 29;
            } else {
               var3 = 28;
            }
            break;
         case 4:
         case 6:
         case 9:
         case 11:
            var3 = 30;
            break;
         default:
            var3 = 0;
      }

      return var3;
   }
}
